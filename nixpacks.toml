[phases.setup]
nixPkgs = ["python312", "nodejs_22", "nodePackages.npm"]

[phases.install]
cmds = ["pip install -r requirements.txt", "cd frontend && npm install"]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "python -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
